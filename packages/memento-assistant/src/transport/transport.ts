// Stub for Task 2 — replaced in Task 3 (Transport interface + MockTransport).
// Keeping this empty placeholder lets `types.ts` reference Transport via
// `import('./transport/transport.js').Transport` without a missing-module error.
export interface Transport {}
