/**
 * Memento DB schema version (latest applied migration).
 * Keep in sync with the highest migration in
 * infrastructure/database/sqlite/migration/migrations/.
 */
export const MEMENTO_LATEST_SCHEMA_VERSION = '37.0';
