# RelationGraph API specification

## Overview

The `RelationGraph` service stores and manages semantic relations between memories. It supports causal, dependency, temporal, and contextual relations and is used by search ranking and the anchor system.

For the full interface definitions, methods, and examples, see the [Korean version](../ko/relation-graph-api.md).
