# Issue 211 Node 24 CI — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Move all GitHub Actions workflows to Node 24, bump official action majors, align `engines` and contributor docs to Node ≥ 24, and keep CI green.

**Architecture:** Workflow YAML edits only (checkout v6, setup-node v6, upload-artifact v6, cache v5, softprops/action-gh-release v3); monorepo root and workspace `package.json` `engines`; documentation strings; optional latency test tolerance for non-CI Node 24 jitter.

**Tech Stack:** GitHub Actions, npm workspaces, Vitest, Node.js 24.

---

### Task 1: Workflows — Node runtime and action pins

**Files:**
- Modify: `.github/workflows/ci.yml`
- Modify: `.github/workflows/release.yml`
- Modify: `.github/workflows/security-check.yml`
- Modify: `.github/workflows/relation-engine.yml`

- [x] **Step 1:** Replace `actions/checkout@v4` → `@v6`, `actions/setup-node@v4` → `@v6`, all `node-version: '20'|'20'` → `'24'|'24'`.
- [x] **Step 2:** In `ci.yml`, `actions/upload-artifact@v4` → `@v6`. In `security-check.yml`, `actions/cache@v3` → `@v5`. In `release.yml`, `softprops/action-gh-release@v1` → `@v3`.
- [x] **Step 3:** Commit message example: `ci: node 24 and bump actions majors (#211)`

### Task 2: Engines — monorepo

**Files:**
- Modify: `package.json` (root `engines.node`)
- Modify: `packages/memento-core/package.json`, `packages/memento-server/package.json`, `packages/memento-client/package.json`, `packages/mcp-client/package.json`, `apps/experimental-example/package.json`

- [x] **Step 1:** Set `"node": ">=24.0.0"` everywhere `"node": ">=20.0.0"` appeared.

### Task 3: SC-002 isolation smoke (Vitest)

**Files:**
- Modify: `packages/memento-core/src/test/test-sleep-consolidation-isolation.spec.ts`

- [x] **Step 1:** For `process.env.CI !== 'true'`, widen `multiplier` from `2` to `3` and `jitterMs` from `80` to `150` so local Node 24 runs do not flake on consolidation overlap.

### Task 4: Documentation and changelog

**Files:**
- Modify: `AGENTS.md`, `DEVELOPMENT_RULES.md`, `README.md`, `README.en.md`, `CONTRIBUTING.md`, `.cursor/rules/specify-rules.mdc`, `.specify/memory/constitution.md`, `docs/guides/ko/developer-guide.md`, `docs/guides/en/developer-guide.md`, `docs/guides/ko/cursor-mcp-setup.md`, `docs/api/ko/api-reference.md`, `docs/reference/ko/Memento-M1-DetailSpecs.md`, `docs/reference/en/Memento-M1-DetailSpecs.md`, `docs/operations/ko/troubleshooting-node-version.md`, `CHANGELOG.md`

- [x] **Step 1:** Replace Node 20 / ≥20 / 20+ phrasing with 24 equivalents in the above list.

### Task 5: Verification

- [x] **Step 1:** From repo root: `cd .worktrees/issue-211-ci-node24 && npm ci && npm test`
  - Expected: all tests pass (exit 0).
- [x] **Step 2:** `npm run lint`
  - Expected: exit 0.

### Task 6: Commits

- [x] **Step 1:** Stage and commit implementation (may be one or two commits after verification).

