---
description: Project Development Guidelines
globs: ["**/*"]
alwaysApply: true
---

# memento Development Guidelines

상세: [AGENTS.md](../../AGENTS.md) · [docs/agents/](../../docs/agents/README.md)

Last updated: 2026-09-06

## Active Technologies
- TypeScript 5.x (tests), vanilla JS (static dashboard), Node.js ≥24 + none new; Vitest + `node:vm` harness (668-883-fix-dashboard-async-anchor)

- TypeScript 5.x, Node.js ≥24, ES modules, npm workspaces
- `packages/memento-core` (`@memento/core`), `memento-server`, `@jee1/memento-client`, `@jee1/memento-assistant`, `@memento/agent-integration`
- SQLite (`better-sqlite3`, `sqlite-vec`), Zod, Vitest, Express 5.x
- Admin UI: `static/js/`, D3.js; 스타일은 `static/css/tokens.css` 토큰 우선
- LLM use-case provider overrides (658 / #820): `LLM_PROVIDER_TRIPLE_EXTRACTION` / `_RELATION_EXTRACTION` / `_PROCEDURAL` + FR-004 model binding in `llm-model-resolver`

## Project Structure

```text
packages/
  memento-core/src/domains/   # memory, search, embedding, forgetting, anchor, relation, procedural
  memento-server/             # MCP stdio + Express HTTP admin
  memento-client/
  memento-assistant/
  memento-agent-integration/
apps/experimental-example/
tests/
static/                       # admin dashboard
scripts/
```

## Commands

```bash
npm install && npm run build && npm test
npm run lint && npm run type-check   # 커밋 전 필수
npm run dev          # MCP
npm run dev:http     # HTTP admin
```

Docker·DB 운영: `npm run db:backup`, `db:pre-docker-deploy` — [commands.md](../../docs/agents/commands.md)

## Code Style

TypeScript strict, 2-space indent, kebab-case 파일명 — [DEVELOPMENT_RULES.md](../../DEVELOPMENT_RULES.md)

## Recent Changes
- 668-883-fix-dashboard-async-anchor: Added TypeScript 5.x (tests), vanilla JS (static dashboard), Node.js ≥24 + none new; Vitest + `node:vm` harness

- 660-807-fts-or-prefix (#807): FTS short queries use OR + prefix* (min stem 2); plan in `specs/660-807-fts-or-prefix/`
- 658-llm-provider-use-case-override (#820): per-job `LLM_PROVIDER_*` for triple/relation/procedural; FR-004 cross-provider model guard; Ollama readiness when job override selects ollama — plan in `specs/658-llm-provider-use-case-override/`

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
