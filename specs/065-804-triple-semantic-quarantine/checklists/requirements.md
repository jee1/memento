# Specification Quality Checklist: 자동 triple semantic 23,664건 격리

**Purpose**: Validate specification completeness and quality before proceeding to planning
**Created**: 2026-08-22 (brainstorm 1~32회차 완료 2026-08-22)
**Feature**: [spec.md](../spec.md)

## Content Quality

- [x] No implementation details (languages, frameworks, APIs) — 조건부 통과, 아래 Notes 1 참조
- [x] Focused on user value and business needs
- [x] Written for non-technical stakeholders — 조건부 통과, 아래 Notes 2 참조
- [x] All mandatory sections completed

## Requirement Completeness

- [x] No [NEEDS CLARIFICATION] markers remain
- [x] Requirements are testable and unambiguous
- [x] Success criteria are measurable
- [x] Success criteria are technology-agnostic (no implementation details) — 조건부 통과, Notes 1
- [x] All acceptance scenarios are defined
- [x] Edge cases are identified
- [x] Scope is clearly bounded
- [x] Dependencies and assumptions identified

## Feature Readiness

- [x] All functional requirements have clear acceptance criteria
- [x] User scenarios cover primary flows
- [x] Feature meets measurable outcomes defined in Success Criteria
- [x] No implementation details leak into specification — 조건부 통과, Notes 1

## Notes

1. **운영 식별자 유지는 의도적이다.** FR-001의 대상 판별식, FR-005의 `forget`·`POST /admin/introspection/heal`, FR-006의 `memory_embedding`·`memory_item_vec_*`·`memory_item_fts`, FR-008의 `npm run db:pre-docker-deploy`, FR-010의 `VACUUM`은 구현자가 고를 수 있는 선택지가 아니라 이슈 #804가 못박은 **제약**이다. 특히 FR-005("기존 도구 사용, 신규 기계 금지")는 도구 이름을 지우면 요구 자체가 사라진다. 이를 추상화하면 스펙이 이슈보다 약해지므로 그대로 둔다. 저장소 선례도 같다 — `specs/064-735-perf-recall-metadata-fts-vector/spec.md`가 모듈 경로와 함수명을 그대로 싣는다.
2. **독자는 운영자다.** 프로덕션 DB 유지보수 작업이라 최종 사용자용 서술이 성립하지 않는다. User Story는 모두 운영자 관점의 결정과 확인 행위로 작성했고, 각 시나리오는 DB 내부 지식 없이도 "무엇을 확인하고 무엇이 보장되는가"로 읽힌다.
3. 검증 1회차에서 모든 항목 통과. brainstorm 세션 후 재검증에서도 전 항목 통과.
5. **brainstorm 1회차(2026-08-22)**: 코드 조사로 FR-004·FR-005의 전제가 틀렸음이 확인되어 재작성했다. 신규 FR-001a·003a·004a·005a~c·006a·008a·009a, SC-001a·003a·005a·006a 추가.
6. **brainstorm 2회차(2026-08-22)**: 1회차가 남긴 내부 모순 4건(US1 본문 vs FR-004a, US2 시나리오 2의 pinned 미반영 숫자, SC-002 절대값 vs Assumption, US3 Independent Test)을 재집계 기준으로 통일했다. `memory_item.subject` 쓰기 경로 전수 조사로 판별식이 가정이 아닌 코드 보장임을 확인해 FR-002를 강화했다. 미탐색 카테고리 3건(프라이버시·성능·경계 조건)을 다뤄 FR-002a·004b·006b·006c, SC-007a·007b·008a 추가. Edge Cases 11 → 15건. FR 20 → 24개, SC 12 → 15개.
7. **brainstorm 3회차(2026-08-22)**: 새 카테고리 대신 스펙이 주장하는 전제를 코드로 검증했다. `db:pre-docker-deploy` 실재 확인, `event_outbox` 필터 조건 구체화. 어긋난 것 여섯 건 수정 — 백업 위치가 기존 `db:backup`(`<DB_PATH>/backups/`)과 충돌, `db:restore-from-corrupt`가 롤백 도구가 아님, **FR-005와 FR-008a가 서로를 불가능하게 만들고 있었음**(서버 정지 상태에서 MCP 도구 호출 불가 — 제약을 "러너 외 쓰기 프로세스 없음"으로 재서술), SC-006에 검증 경로 부재(복원 리허설 추가), US1 Independent Test의 사본/라이브 불일치, VACUUM 디스크 요구량 누락. FR-005d·007a·007b 추가로 FR 27개, SC 15개, Edge Cases 15건.
8. 세 차례 brainstorm 후에도 Content Quality·Requirement Completeness·Feature Readiness 전 항목 통과. Notes 1·2의 조건부 통과 사유는 그대로 유효하다.
9. **brainstorm 4회차(2026-08-22)**: `memory_item` 참조 테이블 전수 조사와 subject 쓰기 경로 재검증. **2회차 결론이 틀렸음을 확인** — `convert_episodic_to_semantic`이 `semantic-memory-update-pipeline`을 거쳐 같은 `createSemanticMemory`에 도달하므로 사람이 호출한 도구의 산출물도 판별식에 걸리고 DB상 구분이 불가능하다(FR-002b·002c, SC-003b). 참조는 CASCADE 8개 / SET NULL 4개 / FK 없음 1개(`memory_forgetting_event`)로 갈리며, SC-005의 "잔재 0행"이 어느 집합인지 불명확했던 것을 명시했다(FR-006a 확장, FR-006d·006e). `memory_provenance`는 agent promotion 경로만 쓰므로 FR-004a는 유효. FR 31개, SC 16개, Edge Cases 17건.
10. **brainstorm 5회차(2026-08-22)**: 4회차 발견이 스펙 전반에 반영되지 않은 것을 정리했다. **오탐의 정의가 없었던 것**이 핵심 — convert 산출물이 "자동 추출 vs 사람이 직접 작성" 이분법의 어느 쪽도 아니어서 SC-003·FR-002a가 FR-002b와 어긋났다. 판정 기준을 호출 주체가 아닌 문장의 출처로 못박았다(FR-002a 재작성). 대상 지칭을 "파이프라인 산출 triple semantic"으로 통일(12곳). 서버 정지의 세 번째 이유(망각 정책이 격리 대상 아닌 기억을 지움 → SC-002·FR-011 위협)와 정지 유지 범위(US3 완료까지)를 추가(FR-008a 보강, FR-008b). FR 32개, SC 16개, Edge Cases 18건.
11. **brainstorm 6회차(2026-08-22)**: 4·5회차가 넣은 내용을 검산했다. **인용 수치 오류** — FR-002b가 에픽 #803의 "85%"를 격리 대상(23,664) 모수로 잘못 읽어 구간 밖을 "약 3,500건"이라 했으나, 85%는 semantic 전체(25,520) 대비 값이고 대상 모수로는 91.3%, 차이는 2,057건이다. 고정 숫자를 빼고 실행 시점 재집계로 넘겼다(FR-002e). **표본 둘의 관계 미정의** — 표본 A(전체 50)/B(구간 밖 50) 독립 100건으로 확정(FR-002d); 구간 밖이 1할 수준이라 표본 A만으론 평균 4건만 걸려 검증이 성립하지 않는다. 그 외 5회차 sweep 누락 2곳, US2 Independent Test의 SC-006 미반영, SC-004의 대상 DB 미명시를 정렬. FR 35개, SC 16개, Edge Cases 19건.
12. **brainstorm 7회차(2026-08-22)**: Problem Statement의 세 숫자(23,664 / 23,651 / 23,649)가 어긋나는 것을 단서로 **#768 원문 폴백 행**을 찾았다. `tripleToNaturalLanguage`는 재조립 실패 시 원본 episodic 본문을 content로 저장하므로 격리 대상 일부는 사람이 쓴 문장을 본문으로 갖는다. 5회차가 정한 "오탐 = 사람이 본문을 직접 쓴 서술"이 이 행에서 **잘못된 중단**을 유발한다. 오탐을 `subject` 기준(판별식 결함)으로 재정의하고, 표본 검토의 역할을 오탐 판정에서 본문 세 형태 분포 확인으로 바꿨다(FR-002a 재작성, FR-002f·002g). "템플릿 문장"으로 대상을 부르던 표현들도 형태 2·3을 배제하므로 `subject` 기준으로 교체. FR 38개, SC 16개, Edge Cases 20건.
13. **brainstorm 8회차(2026-08-22)**: 7회차가 형태 (2)의 규모를 "10여 건"으로 단정한 근거가 성립하지 않음을 확인했다(이슈 표의 15,067/8,584는 템플릿 문장 **내부** 분류). `buildTripleSentence`는 predicate 끝 글자가 한글이 아니면 즉시 null이라 영문·기호 predicate가 많으면 형태 (2)가 수천 건일 수도 있다. 형태 (2)의 본문은 사람이 쓴 원문이므로 비중이 크면 "정보량 0" 논거가 무너진다 → **형태 분포를 전수 집계로 구하고 형태 (2)가 5% 초과면 실행 중단**하는 게이트를 넣었다(FR-002g 재작성, FR-002h, SC-003c). 7회차 sweep 누락 2건(Edge Case의 5회차 정의 인용, FR-002 리드 문장) 정렬. FR 39개, SC 17개, Edge Cases 20건.
14. **brainstorm 9회차(2026-08-22)**: 라이브 DB를 읽기 전용으로 조회해 스펙의 미지수를 실측으로 확정했다(DB 무변경). 형태 (2) = **106건 0.44%**로 5% 게이트 통과, 그중 105건은 원본 episodic이 살아 있어 실질 손실 1건. 형태 (3) 0건, `pinned` 대상 0건, `event_outbox` 0행, `anchor` 3건. 대상은 23,664 → **24,192**로 증가 중이라 FR-008a(서버 정지)의 필요성이 실증됐다. 「실측 기준선」 섹션 신설, FR-004c 추가, FR-001a·002f·002h·004a·009a·002d에 실측 주석. FR 40개, SC 17개, Edge Cases 20건.
15. **brainstorm 10회차(2026-08-22)**: FR이 dry-run에 요구하는 검증 항목을 읽기 전용으로 사전 실행했다. FR-004 코퍼스 대조 통과(교집합 0, kg_triple 보존 **100%**). **`memory_forgetting_event` 고아가 225,617행·37.7MB**(전체의 81%, 전부 `action='review'`, 대상당 9.3회)로 드러나, 규모를 모른 채 "#810 범위, 손대지 않는다"로 정했던 FR-006d를 뒤집어 정리 대상에 넣고 FR-006f로 범위를 한정했다. 회수 구성: 임베딩 194.8MB + forgetting_event 37.7MB + 본문 1.0MB. 에픽 수치 검증(importance≥0.8 8,666 vs 8,600; 40자 미만 14,276 vs 13,977). FR 41개, SC 18개.
16. **brainstorm 11회차(2026-08-22)**: FR-002c가 요구하는 구간별 형태 분포를 실측 — **형태 (2) 106건이 전량 백필 버스트 구간 밖**(구간 안 0.00%, 구간 밖 3.28%)에 몰려 있어 재논의 조건이 발동했다. 8회차의 5% 게이트 대신 **판별식에서 형태 (2)·(3)을 제외**하는 쪽으로 해결(FR-001 축소, FR-001b 신설, FR-002h 철회, SC-003c 재작성·003d 추가). 격리 대상 24,192 → **24,086건**, 제외 106건은 ID 목록 보고. `freelist_count` 98페이지로 단편화 거의 없어 VACUUM 회수 추정치 신뢰 가능. FR 42개, SC 19개.
17. **brainstorm 12회차(2026-08-22)**: 11회차 판별식 축소의 파급 정리. **SC-001이 원리적으로 달성 불가능했다** — "subject 보유 semantic 0건"을 요구하는데 FR-001b가 106건을 의도적으로 보존하므로 어떤 실행으로도 통과할 수 없었다. 형태 (1) 기준으로 재정의(US3 Independent Test·시나리오 1·FR-003a 동반 정렬). **표본 B 폐지** — 목적(형태 2 검출)이 FR-001b로 달성됐고 남은 목적(convert 산출물 식별)은 형태 (1)끼리 육안 구분이 불가능해 표본으로 달성 불가(FR-002d 재작성, SC-003b 축소, 표기 8곳). 좁힌 대상 기준 재집계: 임베딩 24,086건·193.96MB, forgetting_event 고아 225,601행. FR 42개, SC 19개.
18. **brainstorm 13회차(2026-08-22)**: 11회차가 넣은 `content LIKE subject || '_ %'`가 **`subject` 값을 LIKE 패턴에 그대로 삽입**해 그 안의 `_`·`%`를 와일드카드로 해석하는 문제를 발견. 실측상 `_` 포함 `subject`가 **941건**이며, naive LIKE·이스케이프·위치 비교 세 방식이 모두 24,086건으로 일치해 현재는 오분류가 없으나 우연이다. **FR-002i 신설**로 위치 비교(`substr`) 강제. 12회차가 놓친 파급도 정리 — 판별식에 형태 조건이 추가됐는데 FR-002a의 오탐 정의는 `subject` 하나만 보고 있어, 오탐을 두 종류(`subject` 결여 / 형태 오분류)로 재정의(SC-003 보강, Edge Case 추가). FR 43개, SC 19개.
19. **brainstorm 14회차(2026-08-22)**: FR-002i의 `+2` 전제를 코드로 검증 — `attachParticle`이 종성·한글 여부와 무관하게 조사를 **정확히 1글자** 붙이므로 성립. `subject`·`predicate`·`object`의 공백·개행 0건이라 `trim` 유무가 결과 불변(각 24,086건). **오탐 판정을 표본에서 전수 검증으로 이관** — 표본 50건은 모수 24,086에서 오탐률 6% 미만(최대 1,445건)만 보장하나, 두 종류 오탐 모두 SQL 전수 확인이 가능하다(FR-002j 신설). 표본 A는 육안 확인 용도로만 존속(FR-002d 재서술, SC-003 재작성, SC-003e 신설). FR 44개, SC 21개.
20. **brainstorm 15회차(2026-08-22)**: 표본 A를 처음으로 실제 추출·검토. **추출 방법 오류 발견** — `ORDER BY id LIMIT n OFFSET random()`이 연속 블록을 뽑아 50건 전부 한 달치였다(FR-002d에 `ORDER BY random()` 명시). 오탐은 0건. **그러나 "정보량 0" 전제가 반증됐다** — 표본 절반 가까이가 파일 경로·이슈 번호·수치·설계 결정을 담았고, object ≥8인 대상 200건 프로브에서 **37%는 object 문자열이 사람이 쓴 기억에 없었다**(약 6,000건 규모). 전제를 "검색 가치 0 + `kg_triple` 100% 보존"으로 재진술하고 손실 규모를 명시(Problem Statement 논거 1 재작성, FR-004 (b) 격상, SC-004a 신설). FR 44개, SC 23개.
21. **brainstorm 16회차(2026-08-22)**: 15회차가 "정보량 0" 대신 넣은 **"검색 가치 0" 논거가 사실과 정반대**임을 실측으로 확인 — 대상은 `importance ≥ 0.8`인 기억 9,994건 중 **8,615건(86%)**을 점유하고 평균 importance(0.564)가 episodic(0.476)보다 높다. 걸리지 않는 게 아니라 가장 잘 걸린다. 논거를 **점유 대비 답변력**("자리를 차지하되 답이 되지 못한다" — 평균 39.9자 vs episodic 359.7자)으로 재진술(Problem Statement 논거 1 전면 재작성, Goals·실측 표 갱신). 보존될 형태 (2)·(3)의 프로파일(297.8자, importance 0.726)이 procedural과 닮아 FR-001b 결정을 뒷받침. 세션 중 형태 (2)·(3)이 106→116건으로 증가.
22. **brainstorm 17회차(2026-08-22)**: FR-003a가 요구하는 `memory_injection` 사전 기록이 **읽기 전용이 아님**을 확인 — `knowledge-context-bundle-builder.ts:244`가 반환 기억의 `recall_count`·`last_accessed_at`·`g_value`·`consolidation_score`를 UPDATE한다. **FR-011 위반**(사람이 쓴 기억의 내용 변경)이자 **SC-001a 오염**(g_value·consolidation_score가 랭킹에 쓰이므로 비율 상승이 격리 효과인지 프로브 효과인지 구분 불가). 프로브를 **사본 A(격리 전)/사본 B(격리 후)** 로 옮겨 라이브 무오염 + 대칭 오염 확보(FR-003a 재작성, FR-003b·003c 신설, FR-011·SC-001a·SC-004 보강). 진단 `recall`에 `auto_set_anchor: false` 명시(#811). FR 50개, SC 24개.
23. **brainstorm 18회차(2026-08-22)**: 17회차의 사본 B 정의("사본 A에 격리 적용")가 **이미 전량 격리 리허설**임을 인식. FR-006c의 100건 배치 추정을 **사본 B 전량 리허설 실측**으로 대체하고, 리허설이 소요 시간·연쇄 정리·VACUUM 회수량·재개 동작을 라이브 전에 모두 검증하도록 함(FR-006c 재작성, FR-006g·SC-008b 신설, SC-007a 재작성). 절차를 6단계로 명시(FR-003a 재작성). 사본 프로브 실행 조건 확인 — `DB_PATH` 지정 별도 서버 인스턴스 필요, 임베딩은 **minilm(로컬 ONNX) 28,427건**으로 외부 API 불필요, online backup이 vec 가상 테이블도 복사(FR-003d 신설). FR 53개, SC 26개.
24. **brainstorm 19회차(2026-08-22)**: 17회차가 검증을 사본으로 옮긴 순간 **FR-008b("US3 검증 완료까지 서버 정지 유지")의 근거가 소멸**했음을 확인 — 대조가 사본 A·B에서 일어나므로 라이브 서버를 올려도 영향이 없다. 근거 없는 제약이 두 회차 동안 남아 서비스 중단을 늘리고 있었다. 정지 구간을 **재집계(FR-004b)부터 `VACUUM` 완료(FR-010)까지**로 좁히고 백업·리허설·프로브는 서버 가동 중 수행(FR-008a 범위 한정, FR-008b 재작성, FR-008c 신설, SC-008c, US2 story·시나리오 재정렬). 리허설이 길수록 ±5% 게이트에 걸릴 확률이 커지는 긴장은 Edge Case로 명시. FR 55개, SC 27개.
25. **brainstorm 20회차(2026-08-22)**: 19회차 교훈을 사본 구조 전체에 전수 적용. **FR-007b(복원 리허설)가 사본 A 검증과 중복** — 사본 A가 곧 백업 파일이고 거기에 서버를 물려 프로브 10회를 돌리므로, 구동 성공이 건수 대조보다 강한 무결성 증명(스키마·인덱스·vec·FTS 전부 필요)이다. 구동 검증으로 통합(FR-007b·SC-006 재작성). **롤백 절차 구체화** — "사본 A를 라이브 경로로 교체, `-wal`·`-shm` 포함"(FR-007a, US2 시나리오 7). **SC-006a 검증 장소 확정** — 중단 후 재개는 사본 B 리허설에서 의도적으로 한 번 중단해 확인, 라이브에선 안 함. Key Entities에 사본 A(3역: 롤백 근거·무결성 검증·before 프로브)·사본 B 명시. FR 47블록, SC 26개.
26. **brainstorm 21회차(2026-08-22)**: 디스크 요구량을 확인하려다 **롤백 근거가 껍데기일 위험**을 발견 — `~/.memento/data/backups/`에 6,883개·5.0GB가 쌓였는데 546MB 정상 백업은 1개뿐이고 대부분이 **262,144바이트**(64페이지)다. FR-007이 이 도구 산출물을 롤백의 유일한 근거로 삼고 있었다. **FR-007c(크기 대조 1차 게이트)** 신설 — 백업 직후 사본 A 크기를 라이브와 대조해 현저히 작으면 즉시 중단. 256KB 백업은 스키마가 온전해 서버 구동엔 성공할 수 있으므로 크기 게이트가 FR-007b 구동 검증보다 **앞에** 와야 한다. 누적 정리·반복 호출 원인 규명은 Non-Goals로 분리. 디스크 여유 493GB로 사본 2개+VACUUM 충분. FR 48블록, SC 26개.
27. **brainstorm 22회차(2026-08-22)**: **21회차 귀속 오류 정정** — 262,144바이트 파일들은 `db:backup`이 아니라 `backup-manager.ts:80`의 **마이그레이션 백업**(`memory-backup-<버전>-<타임스탬프>.db`)이었다. 실제 분포는 총 6,899개 중 마이그레이션 백업 6,880개 / `db:backup` 산출물 **19개**이며, 후자는 84~115MB로 당시 DB 크기에 맞다. "`db:backup`이 껍데기를 만든다"는 추론 **철회**. FR-007c는 다른 실증 근거로 유지 — `db:backup` 산출물 중 **0바이트 파일 실재** + `-wal`·`-shm` sidecar 잔재(스크립트에 정리 로직이 있는데도). FR-007c 재작성, Edge Case·Non-Goals·실측 표 정정.
28. **brainstorm 23회차(2026-08-22)**: 라이브 DB에 `pragma_foreign_key_list`를 직접 던져 실제 FK를 읽음(4·10회차는 `schema.sql` 기반). **FK 분류 오류 2건** — `agent_memory_promotion_candidate`가 CASCADE(`summary_memory_id`)와 SET NULL(`memory_id`·`merge_target_memory_id`) 양쪽에 걸침(실무 영향 0, 테이블 0행). **10회차 CASCADE 실측이 `memory_relation`을 누락** — 62,289행 중 **54,742행(88%)** 삭제, 양쪽 끝이 모두 대상인 관계는 0건이라 전부 생존 기억과의 연결이 끊김(FR-006h). **FR-004a가 틀렸음** — 관계 엔진이 `memory_relation`에 `extracted_from` 출처 링크를 기록해 왔고 격리 대상에 걸린 것이 **25,069건**(FR-004a 정정). 16회차의 "kg_triple 100% 보존" 논거가 관계를 커버하지 않으므로 격리 전 ID 수준 내보내기 추가(FR-006i, SC-005c, Problem Statement 논거 2 보강). FR 51블록, SC 27개.
29. **brainstorm 24회차(2026-08-22)**: 23회차 교훈을 나머지 스키마 기반 결론에 적용 — **트리거 6개가 라이브에 전부 실재**함을 확인해 FR-006 근거 검증. `memory_embedding_vec_delete`는 **provider 분기 없이** 5개 vec 테이블을 모두 지우고 `rowid = OLD.id`를 올바르게 씀(minilm 이중 적재도 커버; `forget-tool.ts`의 TEXT id 버그와 대비). `memory_item_vec_mock`은 라이브·트리거 양쪽에 없어 일치. **검증 수단 공백 발견** — `sqlite3` CLI에 `vec0`·`fts5` 모듈이 없어 SC-005의 "벡터·FTS 잔재 0행"을 확인할 방법이 스펙에 없었다. 확장 로드 경로 명시(**FR-006j** 신설, SC-005 보강, Edge Case 추가). FR 51블록, SC 27개.
30. **brainstorm 25회차(2026-08-22)**: 판별식 실행 계획과 WAL을 확인 — 둘 다 문제 없음(인덱스 최적화 여지 없으나 26,137행 훑어 24,086건 반환이라 무의미; WAL 2.6MB·autocheckpoint 4MB). 그 과정에서 **파이프라인 악화 발견** — 형태 (2) 월별 폴백률이 05 **0.0%** → 06 **0.0%** → 07 **0.1%** → **08 11.6%**(115/991)로 급증, 형태 (2) 116건 중 115건이 이번 달 것. **11회차 FR-001b의 제외 근거("0.44%라 무시 가능")가 흔들린다** → 재검토 조건 추가, dry-run이 월별 추이 보고(**FR-001c** 신설). 원인 규명은 Non-Goals로 분리하고 별도 이슈 제기(#805·#811 인접). FR 52블록, SC 27개.
31. **brainstorm 26회차(2026-08-22)**: 25회차가 Non-Goals로 뺀 폴백 원인을 최소한만 확인 — **형태 (2) 116건 전부 predicate 마지막 글자가 비한글**(`conjugatePredicate`가 null 반환하는 조건). 같은 8월 형태 (1)은 89%가 한글 종결이므로 **코드가 아닌 predicate 데이터 성격 변화**. predicate가 구(phrase)화(평균 8.2자, 32건 공백 포함 vs `kg_triple` 평균 4.1자). 판별식은 영향 없음. **`kg_triple`도 같은 오염을 받음** — 보존율 100% 재확인(24,086/24,086, 형태 (2) 116/116)이지만 품질은 별개. **FR-004 (b)의 보장 범위를 "조합의 존재이지 품질이 아님"으로 명확화**(FR-004d 신설: 정규화 지표 스냅샷, SC-004a 갱신·SC-004b 신설). FR 53블록, SC 29개.
32. **brainstorm 27회차(2026-08-22)**: 판별식이 보지 않는 컬럼(`is_deleted`·`deleted_at`·`project_id`·`owner_id`·`privacy_scope`)을 실측 — 대상 24,086건 **전부** NULL/NULL/private이고 소프트 삭제 0건. **코퍼스에는 프로젝트 7개·소유자 2명이 존재**하므로 판별식이 별도 조건 없이 귀속 기억과 team·public을 자연히 제외하고 있음이 확인됐다. 귀속 조건을 판별식에 박지 않기로 결정(FR-002c의 시기 조건 논리와 동일 — 박으면 나중에 프로젝트별 산출물만 영구히 남음), 대신 dry-run이 분포를 보고해 감시(**FR-001d·001e** 신설, SC-003f, FR-011 보강, Edge Case 추가). FR 55블록, SC 30개.
33. **brainstorm 28회차**: 격리가 **재추출까지 막음** 발견 — episodic 2,982건이 `triple_extracted=1/success`이고 격리는 이를 건드리지 않아 재추출 스킵. 리셋하지 않고 복구 경로만 문서화(FR-006k·006l) — 23회차의 `memory_relation.extracted_from` 25,069건이 그대로 복구 목록. 대상의 `source`·`origin_source`·`tags`·`session_id` 전부 0건(4회차 결론 재확인). `memory_review_candidate` 3,609/5,300(68%) 삭제 — 10회차 CASCADE 실측 누락분.
34. **brainstorm 29회차**: 대상의 **실제 반환 기록** 측정 — 739/24,086(3.1%)만 반환된 적 있고 `importance ≥ 0.8`인데 한 번도 반환 안 된 대상이 **8,285건**. **지표 함정 발견** — `remember` 경로는 `recall_count=1`로 시작하고 `createSemanticMemory`는 0이라 타입 간 직접 비교 불가(FR-001f). 16회차의 "밀어낸다"를 "높은 우선순위를 차지한 채 3.1%만 반환된다"로 정밀화.
35. **brainstorm 30회차**: 템플릿 필수 섹션 전부 충족 확인. 헌법 v1.2.0 코퍼스 조항과 FR-006b의 연계 명시. **「실행 절차 요약」 섹션 신설** — 판별식 SQL·6단계 절차표·중단 게이트 8개·정지 구간·필수 내보내기를 FR 번호 색인으로 정리(중복 서술 아님).
36. **brainstorm 31회차**: 성공 기준 26개 전수 감사 — **검증 장소 미명시 7건**(SC-003·003e·004a·005c·007·007b·008)에 판정 시점 표기 추가(dry-run/사본/라이브/상시), SC 순서 정렬(FR은 매 회차 정렬했으나 SC는 누락). `- **SC-…**` 30건 중 4건은 Brainstorm Log 인용이므로 본문 기준은 26개.
37. **brainstorm 32회차 — 종료**: 「실행 절차 요약」과 본문 대조 결과 일치(판별식·게이트 8개·절차 6단계·핵심 수치). FR-001 모수만 갱신(24,192 → 24,202, 세션 중 변동 자체가 FR-004b 재집계의 근거).
38. **브레인스토밍 종료**. 31·32회차가 새 사실 없이 정리만 했고, 남은 미지수는 실행 소요 시간 하나뿐이며 사본 B 리허설(구현)에서만 나온다. 라이브 DB 측정(코퍼스·형태·FK·트리거·관계·귀속·반환기록·시계열)과 코드 확인(`forget`·`heal`·`triple-sentence`·`knowledge-context-bundle`·`backup`) 모두 소진. 다음 단계는 `/speckit.plan`.
39. 미탐색으로 남은 카테고리는 UX 하나이며, 중단 조건(FR-002a·002c·004b·007b·008)이 운영자 혼동을 이미 덮고 있다.
4. 스펙에 [NEEDS CLARIFICATION] 마커가 없는 이유는 이슈 #804와 에픽 #803이 대상 판별식·건수·도구 제약·검증 기준을 모두 명시했기 때문이다. 남은 해석 여지는 Assumptions 섹션에 기록했다(격리=백업 기반 완전 삭제, 스펙 범위=이슈 전 범위).
