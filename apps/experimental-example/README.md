# experimental-example

실험용 앱 예시. **연결 방식: 라이브러리(in-process)**.

- **의존**: `@memento/core`
- **환경**: DB 경로는 `DB_PATH` 환경 변수 또는 `createMementoCore({ dbPath })` 인자로 전달.

Phase 5에서 실제 recall/remember 호출 예시가 추가될 예정.
