{
  "name": "experimental-example",
  "version": "0.0.1",
  "description": "Experimental app using @memento/core (in-process). See README for connection type.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "@memento/core": "*"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
