# [도메인 마이그레이션] Shared 모듈 및 최종 정리

**Parent Issue**: #45

## 📋 목표

Shared 모듈 관련 파일들을 `src/shared/` 디렉토리로 이동하고, 전체 마이그레이션을 마무리합니다.

## 📦 마이그레이션 대상 파일

### Types
- `src/types/*` → `src/shared/types/`

### Utils
- `src/utils/*` → `src/shared/utils/`

### Interfaces
- `src/interfaces/*` → `src/shared/interfaces/`

### Constants
- `src/constants/*` → `src/shared/constants/`

### Config
- `src/config/*` → `src/shared/config/`

## 🔄 마이그레이션 단계

1. **디렉토리 생성**
   ```bash
   mkdir -p src/shared/{types,utils,interfaces,constants,config}
   ```

2. **파일 이동** (git mv 사용)
   - Types 파일 이동
   - Utils 파일 이동
   - Interfaces 파일 이동
   - Constants 파일 이동
   - Config 파일 이동

3. **Import 경로 업데이트**
   - 모든 도메인에서 shared 모듈 import 경로 수정
   - TypeScript path alias (`@/shared/*`) 설정 확인

4. **테스트 실행**
   ```bash
   npm test
   ```

5. **최종 검증**
   - 모든 테스트 통과 확인
   - TypeScript 컴파일 오류 확인
   - Lint 오류 확인
   - 빌드 성공 확인

6. **문서 업데이트**
   - `.cursor/rules/project-structure.mdc` 업데이트
   - README.md 업데이트 (필요시)
   - 기타 문서 업데이트

7. **정리**
   - 빈 디렉토리 제거
   - 사용하지 않는 import 제거
   - 코드 정리

## ⚠️ 주의사항

- Shared 모듈은 모든 도메인에서 사용되므로 import 경로 수정 범위가 매우 큼
- TypeScript path alias 설정이 올바른지 확인 필요
- `tsconfig.json`의 paths 설정 확인 및 업데이트

## ✅ 완료 조건

- [ ] 모든 파일이 새 위치로 이동됨
- [ ] 모든 import 경로가 업데이트됨
- [ ] 모든 테스트 통과
- [ ] TypeScript 컴파일 오류 없음
- [ ] Lint 오류 없음
- [ ] 빌드 성공
- [ ] 문서 업데이트 완료
- [ ] 코드 정리 완료

