# [도메인 마이그레이션] Forgetting 도메인 구조 리팩토링

**Parent Issue**: #45

## 📋 목표

Forgetting 도메인 관련 파일들을 `src/domains/forgetting/` 디렉토리로 이동하여 도메인 중심 구조로 전환합니다.

## 📦 마이그레이션 대상 파일

### Algorithms
- `src/algorithms/forgetting-algorithm.ts` → `src/domains/forgetting/algorithms/forgetting-algorithm.ts`
- `src/algorithms/forgetting-algorithm.spec.ts` → `src/domains/forgetting/algorithms/__tests__/forgetting-algorithm.spec.ts`
- `src/algorithms/spaced-repetition*.ts` → `src/domains/forgetting/algorithms/`

### Services
- `src/services/forgetting-policy-service.ts` → `src/domains/forgetting/services/forgetting-policy-service.ts`
- `src/services/forgetting-policy-service.spec.ts` → `src/domains/forgetting/services/__tests__/forgetting-policy-service.spec.ts`
- `src/services/spaced-repetition/*` → `src/domains/forgetting/services/`

### Factories
- `src/factories/spaced-repetition.factory.ts` → `src/domains/forgetting/factories/spaced-repetition.factory.ts`
- `src/factories/spaced-repetition.factory.spec.ts` → `src/domains/forgetting/factories/__tests__/spaced-repetition.factory.spec.ts`

### Tools
- `src/tools/forgetting-stats-tool.ts` → `src/domains/forgetting/tools/forgetting-stats-tool.ts`
- `src/tools/forgetting-stats-tool.spec.ts` → `src/domains/forgetting/tools/__tests__/forgetting-stats-tool.spec.ts`

## 🔄 마이그레이션 단계

1. **디렉토리 생성**
   ```bash
   mkdir -p src/domains/forgetting/{algorithms,services,factories,tools}/__tests__
   ```

2. **파일 이동** (git mv 사용)
   - Algorithms 파일 이동
   - Services 파일 이동 (spaced-repetition 디렉토리 포함)
   - Factories 파일 이동
   - Tools 파일 이동

3. **Import 경로 업데이트**
   - 도메인 내부 import 경로 수정
   - 다른 도메인에서 참조하는 import 경로 수정

4. **테스트 실행**
   ```bash
   npm test -- src/domains/forgetting
   ```

5. **검증**
   - 모든 테스트 통과 확인
   - TypeScript 컴파일 오류 확인
   - Lint 오류 확인

## ⚠️ 주의사항

- `src/services/spaced-repetition/` 디렉토리 전체 이동
- Spaced repetition 알고리즘 파일들이 여러 개 있으므로 모두 확인 필요
- Factory 패턴 사용처 확인

## ✅ 완료 조건

- [ ] 모든 파일이 새 위치로 이동됨
- [ ] 모든 import 경로가 업데이트됨
- [ ] 모든 테스트 통과
- [ ] TypeScript 컴파일 오류 없음
- [ ] Lint 오류 없음

