# log-issue-monitor + docker-compose.issue-monitor.yml
# 이 파일을 복사해 `.env`로 저장한 뒤 값을 채우세요. `.env`는 git에 커밋하지 마세요.
#
# GitHub: Fine-grained PAT는 Issues 읽기/쓰기, classic PAT는 `repo`(비공개 저장소) 또는 `public_repo`(공개만).

GITHUB_TOKEN=
GITHUB_REPOSITORY=jee1/memento
